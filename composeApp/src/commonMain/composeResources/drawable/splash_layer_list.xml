<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Фон (опционально, если хочешь залить всё одним цветом внутри иконки) -->
  <item>
    <shape android:shape="rectangle">
      <solid android:color="#e0e2ec" />
    </shape>
  </item>

  <!-- Твой PNG логотип -->
  <item
    android:drawable="@drawable/ykis"
    android:width="96dp"
    android:height="96dp"
    android:gravity="center" />
</layer-list>

